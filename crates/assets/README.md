# Kavis UI Assets

`kavis-ui-assets`, Kavis UI için varsayılan SVG simge paketini sağlar.

```rust
let app = gpui_platform::application().with_assets(kavis_ui_assets::Assets);
```

## Lisans

Apache-2.0
